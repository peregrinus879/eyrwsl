# Maintenance Ledger - EyrWSL

Unresolved decisions, deferred work, active limitations, and the dated evidence behind them. Durable rules live in `AGENTS.md`, `DEVIATIONS.md`, a skill, a script header, or a test; remove an item here once its rule has moved there.

## Active Limitations

- Git-review context (2026-09-06): source checked on Neovim 0.12.5, Snacks `882c996`, LazyVim `c10948c`, and installed Neo-tree `0bd0eea`; the WSL-pinned Neo-tree `5e076e5` has the same inspected state-manager API. The managed `gd`/`gD`/`gs` mappings use per-call file/explorer context, not a global directory change; direct picker API calls and other mappings retain upstream behavior. Revalidate after picker cwd, root detection, Neo-tree state, or LazyVim mapping changes. Upstream context reports include [Snacks #1639](https://github.com/folke/snacks.nvim/issues/1639) and the distinct subdirectory-jump fix [#2483](https://github.com/folke/snacks.nvim/issues/2483); neither alone proves installed behavior.
- Vault image paste (2026-09-12): the pinned Obsidian 3.16.6 source includes a WSL PowerShell image path, but actual Windows clipboard and destination-path handling remain unverified. Use the [Windows/vault image workaround](setup.md#windows-integration); revalidate on the actual WSL host or after the plugin's image-paste interface changes.
- Native Herdr workspace limits (2026-09-08): `hdw` creates/focuses a new workspace from valid caller context, including populated tabs and inactive source workspaces. Identity/membership/geometry checks and serialization protect cooperating calls, not a server-side atomic transaction or a malicious server. Cleanup may close only verified new split panes before possible input, never any workspace/tab/root/caller; retain every newly created workspace/root on failure and report original/new context. Source/mock checks and 22 real Herdr 0.8.2 cases passed for the current helper in proved private namespaces with dummy tools and real pane PTYs. Coverage includes global-focus handling, generated-shell chaining, cooperating calls, physical cwd, native names, even/odd/offset/gapped geometry, and mutation-free refusals; forced-timeout cleanup left no survivors. This is not attached-UI, real-agent or WSL host evidence. Revalidate after launcher, Herdr CLI/schema, identity, focus or layout changes.
- Deployment preflight does not roll back disk I/O failure or serialize independent Make processes. `twins-pair` attests exact committed twin blobs, not deployment or publication authorization; CI must name the final published pair. Revalidate these limits before introducing concurrent deployments or changing the CI pair protocol.
- `rsw` monitors during transfers and retries transfer failures, but checks reconciliation and monitor failure only between transfers. The reconciliation threshold is 60 seconds since the last successful completion; there is no transfer timeout or bounded shutdown guarantee. A stalled transfer can defer monitor-failure exit indefinitely while its readiness record remains visible. Inspect logs before restarting; logs/readiness do not prove endpoint synchronization. `lsw`/`dsw` manage only watchers started by this implementation, not older processes. Production rsync endpoints and real WSL inotify behavior remain unverified; revisit on host validation or monitor/rsync changes. No `--delete` semantics are implemented.
- Clipboard fixtures mock Neovim and UTF-8 Windows pipes, not Windows PowerShell. The provider's `Set-Clipboard`/`Get-Clipboard`, empty/null handling, and Unicode/CRLF/trailing-newline behavior still require the actual WSL/Windows check below. Paste deliberately strips every CR, including CRLF and standalone CR; do not interpret round-trip checks as byte-for-byte preservation. Revalidate on PowerShell, WSL interop, or Neovim provider changes.

## Open Decisions

- Deferred current decision, audit 8: explicit-target/completion-aware, reference-safe vault rename and promotion are not implemented. Keep current note operations and shared Obsidian plugin specs unchanged; revisit only on H's explicit note-workflow request, coordinating both twins and the vault project's own scripts.
- Deferred current decisions, audits 21 and 22: immutable approved deployments and new trust/disclosure tiers are not implemented. Continue live Stow and existing EyrAgents policy; the canonical [EyrAgents decisions](https://github.com/peregrinus879/eyragents/blob/main/docs/maintenance.md#open-decisions) own any future change. This repo remains the WSL terminal/Windows owner, not the harness or Omarchy desktop owner.

## Deferred Work

- GitHub actual-WSL acceptance (2026-09-11): complete [standard gh/HTTPS setup](setup.md#github-login-and-https), the [all-Projects repository migration](setup.md#all-repositories-under-projects) and the [handoff](handoff.md) on the intended host. Prioritize `eyrie/omasecboot` and `mews/shahynmc`, then cover every discovered clone/remote and explicit push URL while preserving fork roles and common-worktree ownership. Omarchy's local Git metadata changes do not travel through pull. Verify Linux gh's actual credential-storage choice with H, host-local helper settings without tracked-config changes, complete effective HTTPS URL coverage, private-repository read access and fresh-client Git resolution. [Targeted WSL restart and full Windows reboot/sign-in checks](operations.md#github-access) must pass without routine reauthentication. Linux source checks do not attest WSL credential persistence, and public refs can be anonymous. Keep the 42-package baseline; no Windows SSH service/relay setup is required. Remove when actual-host acceptance passes; revalidate after gh, storage, Git include, PATH or WSL startup changes.

- Hermes actual-WSL pass: after pulling both companion repositories, run guarded `make restow`, then invoke `~/.local/bin/hermes --version` from HOME. Confirm uv/Hermes in `mise ls`, Python 3.13 and persistent installer arguments through `make verify`. On the next eligible `mup`, verify the interpreter remains 3.13 without running the wrapper to repair it. Authenticate interactively, deploy EyrAgents from its own clone, and check `hdw ha` / `hdw ha -c` with the real client. Source/argv fixtures do not establish a cold WSL install or update. Revalidate after mise pipx-option, Python requirement or Omarchy installation changes; remove this item when actual-host checks pass.

- Beginner Setup/Troubleshooting commands were syntax/source checked, not executed on a fresh Windows/Arch image. Preserve the documented 42-package baseline and detailed Windows/root/normal-user contexts; revalidate onboarding assumptions on the fresh-image host pass or an upstream installation-contract change.
- Native-workflow host migration remains pending: follow [the WSL handoff](handoff.md) for exact retired-link checks, including the copied `functions/herdr`, fresh-shell activation without desktop recipes/shortcuts, new-workspace `hdw` checks and separately approved tmux package removal. Native Herdr binary/configuration/keymap stay. Source retirement is not an installed-package result. Preserve active sessions, foreign files and old recovery state; remove this pointer and the handoff only when their concrete acceptance checks are complete, leaving unrelated host work below open.
- Move the Omarchy comparison pin: `DEVIATIONS.md` and the `/omasync` skill pin `v4.0.0` while the packaged Omarchy is at 4.0.2-1 (2026-09-03). On the WSL host, run `/omasync` comparing the owned packages between `v4.0.0` and the current release tag, adopt or record each difference, then move the pin in the `DEVIATIONS.md` baseline paragraph and the skill's steps 1 and 3 and completion check. The `mise/` wrappers follow v4.0.2's `omarchy-mise-install` form (`mise use -g --quiet`, added after v4.0.0) minus the cooldown export, a documented deviation; the pin move closes the `--quiet` gap rather than opening one.

## WSL Host Pass

Pending on the actual WSL host. Stop at the first mismatch. H approves package, exact-path cleanup, Windows settings replacement, or host-local repair targets separately; do not inspect credentials or use existing clipboard contents as diagnostics.

1. Confirm WSL2 and the intended normal-user home/clone: `uname -r`, `whoami`, `pwd`, `git status --short --branch`, and `readlink -f ~/.bashrc`. Preserve dirty/untracked work; after H's intended fast-forward update, read the current [setup guide](setup.md) and ledger. Do not run EyrArcHy deployment here. Fresh-image verification must separately confirm the official Arch root first launch, daily user/wheel/sudo, default home, preserved systemd/interop settings, and locale after a targeted restart using the setup guide's Windows/root/user contexts; do not reinstall an existing distro to test onboarding.
2. Use the [setup guide's 42-package baseline and `pacman -T` list](setup.md#4-prerequisites) to identify missing packages; H handles a reviewed full-upgrade install. Inspect `type -a claude codex opencode hermes` and exact executable package ownership before proposing removal. Preserve only a specifically reviewed user launcher under an unused backup name if needed; keep versions stores until verified. Tmux retirement follows its exact handoff and separate package/session review. No blanket package removal, native-install deletion, or auth/config cleanup is a prerequisite.
3. From the deployed EyrWSL clone, run `make require-clone`, `make dry-run`, `make clean`, `make dry-run`, and `make restow` in order, stopping at errors. The guard must establish active WSL2/interop, including a standard or late handler and bounded PowerShell probe. Confirm real managed parents, leaf links, the four `mise` wrappers, and the paranoid-mode fragment; `~/.config/opencode` must remain EyrAgents-owned. Foreign entries and regular files require exact-path preservation/review, not a force/adopt option.
4. Open a fresh normal-user Arch tab outside existing multiplexers. Check `mise settings get paranoid` is `true`, both `OPENCODE_DISABLE_CLAUDE_CODE_SKILLS` and `OPENCODE_ENABLE_EXA` are `1`, and `OPENCODE_DISABLE_EXTERNAL_SKILLS` is absent. Invoke the four explicit wrapper `--version` commands from [setup](setup.md#9-stow) to install binaries as needed, then check mise inventory and resolution. Retain the 24-hour cooldown and existing auth; project mise configs still require explicit trust. Run `make verify` and record actual results, not fixture outcomes.
5. Confirm a fresh shell supplies `herdr` and `hdw`, not `h`/`t` aliases, copied `hdl`/`hdlm`/`hsl`/`hds`, local tmux recipes or Omarchy AI shortcuts. Open a disposable session with `herdr` and fake agents; check [Native Herdr contract](operations.md#native-herdr) for `hdw <cc|cx|oc|ha> [-c]`: new workspace in the current physical directory, native default names/tab `1`, agreed geometry and AI focus. Test populated/inactive valid callers, repeated calls and generated bottom-right-shell chaining; each must create another workspace while preserving existing names/layouts apart from global focus. Bare invocation shows usage; outside-Herdr or invalid-context calls refuse without server startup or attachment. On failure, retain the new workspace/root and original context; only verified new split panes may be removed before possible input, never any workspace/tab/root/caller. Inspect original/new recovery reports and preserve uncertain state. Do not reset existing workspaces or delete old roots/recovery files. Revalidate after launcher, Herdr CLI/schema or Windows Terminal changes; mocks and Linux CI are not actual WSL evidence.
6. In a fresh normal-user shell, use only disposable Git linked worktrees to verify `ga` from subdirectories and alias-safe navigation, `gd` actual-path/branch confirmation, cancellation, dirty/unmerged refusal, non-forced removal, and retained-branch partial failures. Compare against the primary worktree's current HEAD, not an assumed branch name; no production branch removal is a verification prerequisite. Use disposable local rsw source/destination directories to verify alias/`CDPATH`-independent source resolution, changes during successful transfers, logged failures/retries, missed-event reconciliation, `lsw`/`dsw`, and preservation of destination-only files. Inspect stopped-monitor logs before restarting and review older watchers separately; production remote endpoints require their own explicit authorization and evidence. Fixtures do not establish every interactive Git condition, real WSL inotify, or production synchronization. Retain failures and revalidate after Git, shell alias, helper, inotify, or rsync changes.
7. In a fresh Neovim session, round-trip disposable ASCII, Arabic/CJK, accented/supplementary-plane text, multiline/CRLF, standalone CR, empty/null contents, and trailing newlines through both `+`/`*` registers and a Windows application. Expect PowerShell-only UTF-8 copy/paste without profile side effects and removal of every CR on paste, not byte-for-byte preservation. Do not inspect pre-existing clipboard contents. Record real Windows PowerShell/WSL/Neovim versions, tested revision, and actual results; mocks do not close this check. Retain unresolved failures and revalidate after provider or interop changes. Check `:LazyHealth`/`:checkhealth`, locked-plugin bootstrap, Gruvbox, and the vault plugin only if the vault is present; note operations remain unchanged. Confirm `~/.config/nvim/lua/plugins/git-review.lua` resolves to this clone. From a non-Git parent, check `Space g d`/`Space g s` across two repositories and a selected Neo-tree repository folder without manual directory changes; known non-Git selections must warn and `:pwd` must stay unchanged. Omarchy's isolated smoke is not WSL host evidence.
8. Run `make wt-diff`. Confirm the actual stable Terminal path, `Microsoft.WSL`/`archlinux` generated profile, intended normal user, Windows-installed font, and palette. Only after H reviews full-file replacement, run `make wt-push`, then `make wt-diff`; verify the reported adjacent backup, rollback, and any explicitly selected alternate path. Direct `scripts/wt-diff.sh --push` must have the same host/clone protection. Strict JSON remains required. Record real Windows results; source review alone does not establish these assumptions.
9. If adopting/updating EyrAgents, run `id -u` as the normal Linux user before harness deployment. Its current committed WSL procedure assumes UID `1000` for the literal session-root deny; otherwise stop for harness-policy review, without renumbering a populated account or modifying permissions. EyrWSL itself has no UID 1000 restriction. Confirm Linux Node is available as EyrAgents' separate prerequisite and follow [its deployment and WSL verification items](https://github.com/peregrinus879/eyragents/blob/main/docs/maintenance.md#deferred-work) in its own clone. Those items own harness reconciliation, native OpenCode guidance/plugins/auditor, bridges, and canary. Do not repeat the EyrWSL pass recursively when that ledger links back here.
10. Reference updates are separate authorized reference-dependent maintenance, not bootstrap or an automatic deployment step. Preview first with `bash scripts/update-references.sh --dry-run`, obtain approval for new clones/repointing, then run the preservation-first `make refs` update. It must settle listed clones at exact fetched parity, preserve existing local tags/annotations and listed ignored files, and report/keep stale clones, including any unlisted branches/stashes/ignored data. Tag/file conflicts require separate review; no force or manual removal follows automatically. Fixtures do not establish the shared quarry's state. Record preservation/parity evidence without disclosing private values, retain failures, and revalidate after updater or Git transport changes. Report safe versions, exact revision/tree, completed checks and remaining limitations. Remove this host-pass section only when its checks are complete or each remaining limitation is retained explicitly.

## Revalidation Triggers

- Each Omarchy release (the newest tag in `~/Projects/quarry/omarchy` after `make refs`): rerun `/omasync` against the pin and decide whether to move it, then `make verify` on the WSL host.
